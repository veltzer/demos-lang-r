#!/usr/bin/Rscript

# This is a first example of using a variable in R.
#
# NOTES:
# - We did not need to declare 'x' in advance.
# - We did not state the type of 'x'.
# - 'x' got the type 'double' as a default.

x=6
x
print(x)
typeof(x)
