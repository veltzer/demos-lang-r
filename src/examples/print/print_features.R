#!/usr/bin/Rscript

# This example shows some of the features of the builtin 'print' function in R.

x <- 1/3
print(x)
print(x, digits=20)

name <- "Mark"
print(name)
print(name, quote=FALSE, row.names = FALSE)
